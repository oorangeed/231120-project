# # 으로 주석처리
# 표준 glob 패턴을 따른다
# 디렉토리는 끝에 /를 이용해 표현
# ! 로 시작하는 경우 예외 처리

# 파일 하나만 무시 : fileName.txt
# 특정 디렉토리 모든 파일 무시 : fileDirectory/
# 특정 디렉토리 특정 파일 무시 : fileDirectory/fileName.txt
# 특정 확장자 모두 무시 : *.txt
# 현재 경로에 있는 fileName 무시 : /fileName.txt
# 특정 경로 안에 있는 모든 fileName 무시 : fileDirectory/**/fileName.txt
# 예외인 경우 : !fileName.txt

# add commit push >> 적용 안 된 경우 캐시 삭제 후 다시 push
# git rm -r --cached .
# git add. 
# git commit -m "커밋메세지"
# git push origin {브랜치명}


node_modules/

.env
